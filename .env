MAIL_HOST=smtp.atgroup.com
MAIL_PORT=587
MAIL_SMTP_SECURE=TLS
MAIL_USERNAME=mandreo@atgroup.com
MAIL_PASSWORD=1221at
MAIL_FROM=mandreo@atgroup.com
MAIL_FROM_NAME="Notificador NIS2"
MAIL_TO=admin@atgroup.com
MAIL_TO_NAME="Administrador"
# // Simple loader de .env
# function load_dotenv(string $path)
# {
#     if (!file_exists($path)) return;
#     $lines = file($path, FILE_IGNORE_NEW_LINES | FILE_SKIP_EMPTY_LINES);
#     foreach ($lines as $line) {
#         $line = trim($line);
#         if ($line === '' || $line[0] === '#') continue;
#         if (strpos($line, '=') === false) continue;
#         list($name, $value) = explode('=', $line, 2);
#         $name = trim($name);
#         $value = trim($value);
#         // Quitar comillas si las hay
#         if ((substr($value,0,1) === '"' && substr($value,-1) === '"') || (substr($value,0,1) === "'" && substr($value,-1) === "'")) {
#             $value = substr($value,1,-1);
#         }
#         // Solo si no existe
#         if (getenv($name) === false) {
#             putenv("$name=$value");
#             $_ENV[$name] = $value;
#         }
#     }
# }

# // Cargar el .env en la raíz del proyecto
# load_dotenv(__DIR__ . '/../.env');
